spring:
  jackson:
    time-zone: Asia/Seoul
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${OCI_MYSQL_HEATWAVE_HOST}:${MYSQL_PORT}/${OCI_PROD_MYSQL_DB_NAME}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${OCI_MYSQL_USERNAME}
    password: ${OCI_MYSQL_PASSWORD}
    hikari:
      minimum-idle: 10
      maximum-pool-size: 20

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Seoul

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations:
      - classpath:db/migration
      - classpath:db/migration_data

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD}

    meilisearch:
      url: ${MEILISEARCH_ENDPOINT}
      api-key: ${MEILISEARCH_KEY}

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            client-secret: ${KAKAO_CLIENT_SECRET}
            redirect-uri: "${app.base-url.prod}/login/oauth2/code/kakao"
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope:
              - openid
            provider: kakao
          apple:
            client-id: ${APPLE_CLIENT_ID}
            client-secret: ${APPLE_CLIENT_SECRET}
            redirect-uri: "${app.base-url.prod}/login/oauth2/code/apple"
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope:
              - openid
              - email
            provider: apple
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: sub
            jwk-set-uri: https://kauth.kakao.com/.well-known/jwks.json
          apple:
            authorization-uri: https://appleid.apple.com/auth/authorize
            token-uri: https://appleid.apple.com/auth/token
            jwk-set-uri: https://appleid.apple.com/auth/keys
            user-name-attribute: sub

jwt:
  access-token-secret: ${JWT_ACCESS_TOKEN_SECRET}
  refresh-token-secret: ${JWT_REFRESH_TOKEN_SECRET}
  access-token-expiration-time: ${JWT_ACCESS_TOKEN_EXPIRATION_TIME}
  refresh-token-expiration-time: ${JWT_REFRESH_TOKEN_EXPIRATION_TIME}
  issuer: ${JWT_ISSUER}

oci:
  tenancy-id: ${OCI_TENANCY_ID}
  user-id: ${OCI_USER_ID}
  fingerprint: ${OCI_FINGERPRINT}
  private-key: ${OCI_PRIVATE_KEY}
  region: ${OCI_REGION}
  passphrase: ${OCI_PASSPHRASE:}
  objectstorage:
    namespace: ${OCI_OBJECTSTORAGE_NAMESPACE}
    bucket: ${OCI_PROD_OBJECTSTORAGE_BUCKET}

firebase:
  credentials-path: ${FIREBASE_CREDENTIALS_PATH}

external:
  api:
    ai-server-ip: ${AI_SERVER_IP}
    cloth-inference-path: ${CLOTH_INFERENCE_PATH}
    style-inference-path: ${STYLE_INFERENCE_PATH}
    cloth-detect-path: ${HISTORY_CLOTH_DETECT_PATH}

swagger:
  username: ${SWAGGER_USERNAME}
  password: ${SWAGGER_PASSWORD}

springdoc:
  default-consumes-media-type: application/json
  default-produces-media-type: application/json
  swagger-ui:
    tags-sorter: alpha
    operations-sorter : method
    path: /swagger-ui
    doc-expansion : none

app:
  oauth:
    redirect-scheme: codive
  base-url:
    dev: ${CODIVE_DEV_BASE_URL}
    prod: ${CODIVE_PROD_BASE_URL}

